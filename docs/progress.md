# 当前进度 · 2026-09-15

## 2026-09-15：归档与仓库上传

用户明确授权将当前相关文件保存到 `E:\Pet-store` 并提交、推送到指定 GitHub 仓库。本次保留源码、测试、文档、原阶段0提示词及已有最终手机测试ZIP；不在本次实现新业务或修改手机兼容性代码。Git提交与推送结果以实际仓库历史及远端核验为准。

用户已提供腾讯云CloudBase静态托管上传截图，根目录下为 `site/`；完整公网地址尚未提供，因此未独立核验线上资源。用户iPhone截图显示正常，阿姨手机截图仅显示未加样式HTML，初始化提示未完成；具体原因尚未确认，也未修复。下一步需要两台手机实际打开的完整URL，再检查资源路径、响应及浏览器情况，不能仅凭截图归因于华为不兼容。

原始附件与手机截图另存于本地忽略目录 `work/references/2026-09-15/`，不上传截图。原始软件完整备份继续保留在原E盘备份目录，未在本次访问或改动；旧源码审查副本与数据库不进入Git。可公开的现有测试包见 [归档说明](../releases/README.md)。

本次重新执行：`node --test tests/*.test.mjs` 21/21通过；`node scripts/check-project.mjs` 通过（38个Git候选文件）。暂存前 `git diff --check` 通过，但不覆盖未跟踪文件；全量暂存后 `git diff --cached --check` 报告原规划第3–5行的Markdown双空格换行，为保留原稿而不改动；另有LF/CRLF提示。归档ZIP的16个条目逐一与原构建文件SHA-256一致，整包SHA-256与历史记录一致。常见私钥、云密钥、GitHub令牌及硬编码密码模式扫描未命中；该模式扫描不构成完整安全审计。原始两份规划附件及10张用户截图已复制到本地参考目录。

以下是2026-09-14各阶段的历史记录，“尚未发布”“没有提交/推送”等描述仅对应当时状态。

## 后续本轮：手机测试包

按用户“打包给华为和苹果测试”请求，在原有阶段0之上增加PWA静态部署包、桌面图标、安装说明、离线缓存、本浏览器演示保存、重置确认、严格文件白名单打包与ZIP哈希验证。视觉风格保留。以下阶段0结果为历史记录；“刷新即重置”现已被本机演示保存替代。

本轮没有访问旧库或备份，没有真实数据、支付、通知、部署账号、Git提交/推送。不是原生安装包或完整营业系统，不能直接将ZIP发到苹果安装；尚需负责人选择并授权HTTPS静态托管发布，未提供公网链接。华为型号/系统与iOS版本未知，未做两台真机验证。详见mobile-test-deployment与mobile-test-report。

## 当前交付范围

已完成本轮阶段0工程准备：原规划审阅与落盘、仓库核实、旧源码与SQLite只读审查、经营规则R01-R15、架构/迁移方案、业务验收A01-A30草案、五页内存交互原型。老板可用性验收尚待实际试用，不宣称已获业务验收。

这轮没有搭完整Vue/Nest/PostgreSQL应用，没有真实数据库迁移、生产发布、商家账号操作、支付、短信或客户通知；没有提交/推送Git。文件留在用户指定仓库工作区供审阅。

## 改动清单

- README.md、AGENTS.md、.gitignore：启动、交接与数据保护约定。
- docs/development-plan.md：用户提供规划原稿（仅标准化换行和末尾空行）；保留建议/待确认性质。
- docs/legacy-audit.md、business-rules.md、architecture.md、migration-plan.md、acceptance.md、prototype.md、progress.md：本轮实测结论与执行准备。
- prototype/index.html、style.css、app.mjs、model.mjs：今天、订单、客人预约、寄养、接送的虚构交互。
- scripts/audit-legacy.mjs、audit-snapshot.mjs：在隔离副本查询结构与数据质量计数，提取经hash验证的源码到忽略目录。
- scripts/verify-backup.mjs：三处精确源目录与备份内容核对。
- scripts/serve-prototype.mjs：仅本机、仅原型文件白名单的预览服务。
- scripts/check-project.mjs、tests/prototype.test.mjs：文档、Git候选、原型交互及预览边界检查。

## 实际检查与测试

环境：Windows / PowerShell / Node v24.15.0 / npm 11.12.1；Git main，origin 为 Pet-store。未在 PATH 找到 psql/docker，未部署数据库。

| 执行 | 实际结果 | 边界 |
|---|---|---|
| git rev-parse --show-toplevel / remote -v / status --short --branch | E:/Pet-store，main 跟踪 origin/main，初始仅 README 干净 | 完成本轮后为本轮新增/修改文件 |
| node scripts/audit-legacy.mjs 指定备份目录 | 13份代码hash匹配；4张表，1会员/1订单/1充值/7目录；integrity_check ok，所查异常计数0，数据库前后hash相同 | 不证明真实/演示性质或本金/赠送归属 |
| node --test tests/prototype.test.mjs tests/audit-snapshot.test.mjs | 12/12通过 | 10个内存模型测试＋1个HTTP白名单测试＋1个WAL隔离回归；不是真实业务E2E |
| node --check prototype/app.mjs / scripts/audit-legacy.mjs | 通过 | 语法检查 |
| git diff --check | 通过；现有Git设置有LF转CRLF提醒 | 不等于已提交 |
| 本地浏览器 127.0.0.1:4173 | 实操五页：独立完成宠物、取消/确认演示收款、寄养往返申请进入今天、冲突/可用延住、填写照护、去程完成返程不变 | 虚构数据，无商户联调 |
| 浏览器390×844视口 | 五页 clientWidth=scrollWidth=375（扣除滚动条），无整页横向溢出；已查看截图；完成后重置视口 | 桌面窄屏测试，不是手机微信真机 |
| 浏览器控制台与刷新 | 未捕获error/warn；刷新恢复初始接送状态 | 当前测试会话范围 |

仓库检查通过：必需文件、R01-R15/A01-A30编号、本地文档链接、敏感文件忽略规则均通过；并非完整密钥扫描。原规划仅换行及尾空行标准化。

备份严格比对返回1：安装102文件和快捷方式相同，当前用户数据28文件全部有相同副本；备份多26个旧缓存和本轮首次只读检查生成的2个SQLite辅助文件。主库hash始终相同，所有备份项保留。已按systematic-debugging定位并改为隔离副本查询，回归通过。差异与边界见legacy-audit；“原始备份无改动”的原定验收项存在上述偏差，不以主库未变代替整个目录未变。

## 未完成与需要的业务事实

见 business-rules 的八组问题。最影响阶段1的是：实际价目/加价、人员工位/耗时、收款退款/混合付款政策、会员储值/赠送及旧数据性质。寄养和接送规则在阶段2前确认；主体、预算、短信和平台权限在相应上线前核查。原始备份路径和老板身份已经明确，无需再询问。

## 下一轮只推进一个完整切片

**阶段1：老板手机“老客/新客开单 → 每宠物洗护履约 → 人工核实收款 → 查询账单”**。

1. 用已确认规则建立npm工作区和单一锁文件，搭Vue/Nest、独立本地PostgreSQL测试环境；不连接其他项目数据库。
2. 建最小账号/权限、customer与pet、服务价目/报价快照、订单及每宠物任务、收款分配和审计；生成OpenAPI契约。
3. 从今天页完成开单、开始/完成/交还；资金操作用整数分、事务和持久幂等，演示与营业配置分开。
4. 按R09/R10决定该切片必须带入的会员储值、混合付款和退款分支；未知余额禁止扣款。不能为省时间丢现有权益。
5. 跑真实PostgreSQL并发和对象权限用例、洗护结算浏览器E2E、恢复演练，再交老板试用。达到此切片不等于全V1完成。

收费和资金规则未确认时只能在隔离虚构测试配置下实施演示，不能宣称可营业。不自行跳到接送全量、微信小程序或平台支付接入。
