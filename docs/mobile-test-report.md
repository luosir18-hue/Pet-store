# 手机测试包验证记录 · 2026-09-14

## 范围

在E:\Pet-store现有未提交阶段0成果上增量修改，保留视觉风格。交付是静态PWA测试包，不是原生App、正式账本或多端后台。只含虚构资料，原始备份本轮未访问。

## 已执行

- Node测试21项通过：原模型、HTTP白名单、旧库隔离回归；新增本机状态恢复/隔离、损坏/版本/配额失败、固定字段重建、寄养非法状态、包白名单/哈希/PNG/MIME、SW离线资源/子目录/缓存隔离、首次安装与更新区分。
- 通过Node语法检查；最终文档链接与Git忽略检查见收尾命令。
- 首个部署产物在本地4180端口加载，显示缓存就绪。开始豆包服务后刷新仍为服务中，糯米仍为已到店，未收款。
- 停掉该预览服务，新的PowerShell HTTP请求无法连接；浏览器重新加载页面成功，五页均可访问。
- 服务关闭时提交虚构预约，刷新后仍显示DEMO-1待老板确认；重置先取消保留1条，确认后刷新恢复0条。
- 服务关闭时390×844视口检查五页，documentElement.clientWidth=scrollWidth=375，无整页横向溢出；查看截图后恢复默认视口。
- 浏览器发现首次安装误显示更新按钮，按systematic-debugging复现并增加失败回归；根因是安装中的waiting状态被当作已有版本更新且未在激活后隐藏。已改为controller＋waiting判断并在controllerchange/ready刷新按钮状态，回归通过。
- ZIP由Windows PowerShell生成，逐条目与产物目录核对SHA-256。每包仅16个文件，包括14个site文件、部署说明和release.json；不含work、旧库、备份或密钥。
- 最终包更新实测：旧缓存页面检测到新版本，点击更新后按钮消失；更新前已保存的豆包服务中状态保留，糯米仍已到店。最终浏览器error/warn日志为空。
- 收尾Node测试21/21，项目文件/文档链接/忽略规则检查通过（35个Git候选），git diff --check通过，仅现有LF/CRLF转换提醒。

## 最终交付文件

- `E:\Pet-store\dist\Pet-store-mobile-test-2026-09-14T12-09-25-150Z.zip`
- 内容版本：`f6e0be9ae4116cdf`
- ZIP SHA-256：`26E50E8514D820FE2FC62AE6AF6A63A1F761BBE4DD0993C9DDE4E333E6F36675`
- 本地最终包预览：`http://127.0.0.1:4180/`，不是发给手机的公网地址。
- 较早的12-04-21试验包保留供本地回溯，不作为本次交付；其首次安装更新提示已由最终版本修正。

## 尚未执行，不作保证

- 未选择或发布任何HTTPS托管，没有可以发给阿姨的公网链接。云端首次打开、开发电脑关机后真机首次访问未测。
- 未取得华为型号、鸿蒙/EMUI版本、iPhone/iOS版本；系统浏览器、桌面入口、软键盘、安全区、飞行模式、缓存回收均未真机验收。
- 本地“服务停机后从缓存打开”不能等价为苹果/华为永久离线可用；浏览器可能清理缓存。
- 未提供登录、真实记账、跨手机同步、短信、支付、客户通知、数据库或云端备份。

下一步需要负责人选择并授权静态HTTPS托管，把site完整发布，再按包内手机说明让两台真机试用。购买、账号条款、域名绑定与公开发布不从“打包”请求推断授权。
