# Pet-store 项目约定

## 业务与协作

- 单店：老板为阿姨；猫狗洗护、美容、寄养、上门接送。以老板减少录入和沟通为目标。
- 工作目录 E:\Pet-store，远端 https://github.com/luosir18-hue/Pet-store。
- 先读 docs/progress.md、docs/business-rules.md，再按用户当前请求推进。阶段 0 是审查与虚构数据原型；文档中的路线图不是生产操作授权。
- 当前用户已授权本轮审查和开始改造。日常可逆开发、测试和文件维护可直接执行，不逐项询问；真实收费、容量、身份归属未知时记录待确认，不能猜成生产规则。
- 不自动提交或推送，不购买/绑定外部服务，不迁移真实营业数据，不发送真实通知或操作资金。

## 数据与工程

- 原始备份：E:\宠物店收银与会员_完整备份_20260914_105953，只读文件访问。禁止直接用SQLite打开（readOnly也可能生成WAL/SHM）；先复制到忽略的work目录再查询。
- 旧源码审查副本位于 work/legacy-source，禁止执行其中清库、压测或导入脚本。
- 数据库、备份、营业照片、私人联系方式、余额明细和密钥不得进入 Git。work/ 是忽略的审查与测试目录。
- 后续推荐 Vue 3/TypeScript + NestJS + PostgreSQL，npm workspaces，REST/OpenAPI；阶段 0 不安装完整框架。原型没有业务后台或身份保护，不能部署作营业服务。
- 客户档案与登录身份分离。对象权限由后台执行；接送人员只看分派任务所需资料。
- 金额用整数分；收付款与会员流水可追溯；操作持久幂等，资金和资源变更在事务内处理。业务状态、宠物交还、财务状态分离。
- 往返接送两段独立；寄养延住重新查资源；每宠物独立履约记录。
- 未运行测试写未执行；原型通过不等于 PostgreSQL 并发、支付联调、迁移或手机真机验收通过。
- 本机 shell 为 PowerShell；脚本不要嵌套 shell，不改变用户无关系统环境。保留未提交改动。
